pages:
    stage: deploy
    
    script:
    - mkdir .public
    - cp -a example* .public
    - cp -a *.js .public
    - cp -a index.html .public
    - mv .public public

    artifacts:
        paths:
        - public
    
    only:
    - master
